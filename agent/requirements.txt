google-adk[vertexai]>=1.16.0
ag_ui_adk>=0.6.5
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
requests>=2.32.0
httpx>=0.27.0
# Upper bound matters: google-adk's mcp_tool imports McpHttpClientFactory, which
# mcp 2.x removed. google-adk only declares mcp<2 under its optional extras, and
# we install google-adk[vertexai], so nothing else holds this back — without the
# bound a fresh resolve picks 2.x and the agent dies at import.
mcp>=1.24,<2
